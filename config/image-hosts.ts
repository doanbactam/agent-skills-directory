const IMAGE_HOSTNAMES = [
  "avatars.githubusercontent.com",
  "public.blob.vercel-storage.com",
] as const

const IMAGE_HOSTNAME_SUFFIXES = [".public.blob.vercel-storage.com"] as const

export { IMAGE_HOSTNAMES, IMAGE_HOSTNAME_SUFFIXES }
